{
  "name": "task-ric",
  "version": "1.0.0",
  "description": "vanilla project with tailwind css",
  "main": "index.js",
  "scripts": {
    "build-css": "NODE_ENV=production tailwindcss build -i src/style.css -o public/css/style.css --minify",
    "build": "npm run build-css",
    "dev": "tailwindcss build -i src/style.css -o public/css/style.css --watch"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "tailwindcss": "^3.4.17"
  },
  "devDependencies": {
    "watch": "^1.0.2"
  }
}
